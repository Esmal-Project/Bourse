function drawHistory(canvas,rows){const ctx=canvas.getContext("2d"),dpr=window.devicePixelRatio||1,w=canvas.clientWidth||800,h=canvas.clientHeight||360;canvas.width=w*dpr;canvas.height=h*dpr;ctx.setTransform(dpr,0,0,dpr,0,0);ctx.clearRect(0,0,w,h);if(!rows.length)return;const vals=rows.map(x=>Number(x.close)).filter(Number.isFinite);if(!vals.length)return;const min=Math.min(...vals),max=Math.max(...vals),pad=28;ctx.beginPath();vals.forEach((v,i)=>{const x=pad+i*(w-pad*2)/Math.max(1,vals.length-1);const y=h-pad-(v-min)/Math.max(1,max-min)*(h-pad*2);i?ctx.lineTo(x,y):ctx.moveTo(x,y)});ctx.strokeStyle="#8fb4c7";ctx.lineWidth=2;ctx.stroke();ctx.fillStyle="#78909c";ctx.font="12px Tahoma";ctx.fillText(String(max),8,18);ctx.fillText(String(min),8,h-8)}window.drawHistory=drawHistory;